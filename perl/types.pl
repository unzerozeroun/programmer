# Déclaration de variable de type entier
my $entier = 1;

# Déclaration de variable de type float
my $decimal = 3.14;

# Déclaration de variable de type string
my $chaine = "Hello, World!";
print $chaine;

# Déclaration de variable de type boolean
my $booleen = true;

# Déclaration de variable de type tableau
my @tableau = ("Hello", "World", "!");
print $tableau[0];

# Déclaration de variable de type dictionnaire
my %dictionnaire = ("clef1", "valeur1", "clef2", "valeur2");
print $dictionnaire{"clef1"};

# Déclaration de variable de type n-uplet
my ($x, $y) = (1, 2);
print $x;

# Déclaration de variable de type n-uplet nommé
my %point = ("x", 1, "y", 2);
print $point{"x"};

# Déclaration de variable de type n-uplet nommé
my %point = ("x" => 1, "y" => 2);
print $point{"x"};
